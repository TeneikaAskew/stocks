# Production dependencies for Cloud Run image
# Dev-only packages (matplotlib, seaborn, plotly, streamlit, jupyter, pytest, black, flake8, mypy, scipy, scikit-learn)
# are intentionally excluded — use requirements.txt for local dev and GitHub Actions.

# Core data processing
pandas>=2.1.4
numpy>=1.26.2
pyarrow>=14.0.0

# Market data fetching (AlphaVantage via REST API)
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
fredapi>=0.5.0
# Yahoo Finance — third date source for the earnings calendar fetcher.
# Calendars.get_earnings_calendar is the bulk API (one paginated call
# covers ~3000 tickers/window vs hundreds of per-ticker calls).
yfinance>=1.3.0

# Options Greeks calculation (used in gcp/fetchers/fetch_etf_options.py and fetch_earnings_options.py)
py_vollib>=1.0.1
# Vectorized BSM IV solver — used by lib/options_greeks.py to compute Greeks
# for SPX (AV does not provide Greeks for cash-settled index options).
py_vollib_vectorized>=0.1.1

# Date and time handling
python-dateutil>=2.8.0
pytz>=2025.1

# API and data formats
protobuf>=4.24.0
websockets>=11.0

# Resilience
tenacity>=8.2.0

# SQL parsing + tabular display — used by gcp/queries/run_query.py
# (the ad-hoc SQL runner that backs both .github/workflows/db-query.yml
# and the db-query Cloud Run Job).
sqlparse>=0.5.0
tabulate>=0.9.0

# Environment management
python-dotenv>=1.0.0

# Google Cloud
cloud-sql-python-connector[pg8000]>=1.7.0
google-cloud-storage>=2.14.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
pandas-market-calendars>=4.3.1

# AI Insights agent pipeline (lib/agents/, gcp/insight_pipeline_job.py)
pydantic>=2.12.0
google-genai>=1.0.0
google-cloud-aiplatform>=1.38.0
google-cloud-tasks>=2.16.0
anthropic>=0.40.0  # optional: Anthropic Claude adapter for agent pipeline

# Discord interactions service (gcp/discord_interactions/main.py)
# FastAPI service that receives slash-command webhooks from Discord,
# verifies Ed25519 signatures, and dispatches Cloud Run Job executions.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pynacl>=1.5.0
google-cloud-run>=0.10.0
