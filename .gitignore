# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Google Apps Script
google-apps-script/node_modules/
google-apps-script/.clasp.json

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Credentials and secrets
credentials/
*.pem
*.key
*-credentials.json
service-account*.json
.gcp-key.json

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Docker
.dockerignore

# Jupyter Notebook
.ipynb_checkpoints

# Python
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# NOTE: Project source modules live in lib/ — do NOT ignore the top-level
# lib directory. Python venvs should use .venv/ instead, which is already
# covered by the venv/.venv/env entries below.
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Data files — backed up to GCS (gs://adept-mountain-474619-d4-trading-data/raw/data/)
data/
*.csv

# Research artifacts ARE committed (small CSVs, reproducible audit trail)
!docs/research/**/data/
!docs/research/**/data/*.csv
!docs/research/**/*.csv
!docs/exec_backtest_data/
!docs/exec_backtest_data/*.csv
!docs/options_exec_backtest_data/
!docs/options_exec_backtest_data/**/*.csv

# Windows nul file (accidentally created by shell redirects)
nul
**/nul

# AlphaVantage no-data markers (local cache indicators)
*.nodata
*with_indicators.parquet

# Reports directory — stored in GCS (gs://adept-mountain-474619-d4-trading-data/raw/reports/)
# Keep the README stub tracked so contributors see where the files live.
reports/
!reports/README.md

# Local Claude scheduler state
.claude/scheduled_tasks.lock

# Personal Claude permission overrides — local-only, not for commit
.claude/settings.local.json

# Claude sub-agent git worktrees — transient, local-only git internals
.claude/worktrees/

# GCP inventory dumps (local only, not for commit)
gcp_inventory.json
gcp_iam.json
gcp_schedulers.json
billing_90d.json
.venv
.superpowers/
