# Issue #702 Follow-ups Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task with superpowers:test-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Close all 24 triaged follow-up findings batched on issue #702 (accumulated from the PR #700–#710 review cycles) in one polish wave.

**Architecture:** Seven small, independently reviewable tasks. Behavior changes are test-first; comment/doc-only items batch into one task with compile/suite gates.

**Tech Stack:** FastAPI + pandas, React 19 + TS + TanStack Query, pytest / vitest / Playwright (hermetic mocks; vite 4321 with playwright.config.ts baseURL patched then REVERTED before commit).

## Global Constraints

- Branch: `fix/702-follow-ups` off main. BRANCH GUARD every dispatch (concurrent session switches branches): `git rev-parse --abbrev-ref HEAD` before starting AND before each commit.
- Commits: single-line conventional, NO Co-Authored-By trailer, NO AI branding. Stage EXPLICIT paths — never `-A` (untracked docs/alphavantage/ + model_analysis.txt belong to another workstream).
- Unit conventions stand: `*_pct` TRUE PERCENT; `win_rate` 0-1 fraction (×100 at render); journal `return_pct` percent; engine internals fractions.
- Rule 3.7: nulls render as em-dashes, never fabricated zeros; loud errors.
- TDD for every behavioral change: failing test first, watch red, implement, watch green.

---

### Task 1: Backend journal hardening

**Files:** Modify `platform/api/routers/journal.py`, `platform/src/routes/JournalPage.tsx` (export path only); Test: extend `tests/test_journal_phase2.py`, extend `platform/tests/journal.spec.ts` only if the export flow has an existing spec (check first; else pytest-only).

Items:
1. **Pipeline-export with active trades**: `JournalTradeExportItem` requires exit fields → an active trade in the list 422s. Fix client-side: `exportPipeline` filters `status === 'active'` rows out of the POSTed list (they have no exits to export) and the toast mentions how many were skipped ("exported N closed trades · M active skipped"). Test: pytest confirming the SERVER contract unchanged (active-shaped item still 422s — pin it) + vitest for an exported pure `exportableTrades(entries)` helper (filters active, keeps closed) if you extract one (do — JournalPage already exports helpers).
2. **`status` Literal**: `JournalTradeCreate.status: Optional[Literal['active','win','loss','breakeven','closed']]`. Test: POST with `status: 'bogus'` → 422 (currently 200/500 — watch red first).
3. **PATCH race guard**: the close UPDATE gains `AND status = 'active'`; check rowcount — 0 rows updated after the SELECT said active → 409 (someone closed it concurrently). Test: monkeypatched `_journal_exec` returning rowcount 0 → 409. (Read how `_journal_exec`/execute_sql reports rowcount — adapt; if it doesn't return one, use a `RETURNING id` fetch via `_journal_query` instead and treat empty as 409.)
4. **Seed `date` validation**: malformed `date` (not `YYYY-MM-DD`) → 422 before the query (currently a Postgres cast error → 503). Test: `/api/journal/seed/IWM?date=garbage` → 422.

Commit: `fix(api): journal export skips active trades; status literal; close race guard; seed date validation`

### Task 2: Backend replay/style + time hardening

**Files:** Modify `lib/backtest.py` (`_aggregate_scorecards`), `platform/src/routes/ChartsPage.tsx` (ScorecardFooter), `lib/signals.py` (`generate_signals`), `platform/api/routers/live.py` (`_normalize_bar_time`), `platform/src/hooks/useJournalChartTrades.ts` (header comment — fold in here since the file is touched); Test: extend `tests/test_replay_labeled_trades.py`, `tests/test_style_walk_forward.py` (generate_signals threading), `tests/test_live_signal_series.py` (ms guard), vitest for footer null-formatting helper if extracted, Playwright scorecard zero-scored case.

Items:
1. **Zero-scored aggregate → nulls**: when `scored_n == 0`, `win_rate`/`avg_return_pct`/`avg_exit_edge_bps` become `None` (not 0.0). ScorecardFooter renders em-dashes for null (win rate '—', avg return '—', edge '—') — the "0/N scored" context line stays. pytest: all-unavailable replay → aggregate nulls (watch red). Playwright: mocked all-unavailable response → footer shows dashes not "0%".
2. **Footer surfaces `system_no_signal_n`**: copy becomes "system had a setup on X of N entries · no setup on Y" when Y > 0. Playwright assertion updated.
3. **`generate_signals` threads `enabled_conditions`**: pass `signal_config.enabled_conditions` into `evaluate_signal` (closing the inert asymmetry). pytest: a restricted config through `generate_signals` produces no fires on a bar where only non-listed conditions are true (mirror the existing walk-forward exposing test at the generate_signals level; watch red).
4. **`_normalize_bar_time` ms guard**: digit strings with >12 chars (epoch-milliseconds and beyond) raise HTTPException 422 with a "milliseconds epoch not supported — send seconds" message rather than parsing to year ~57k. pytest: 13-digit input → 422; 10-digit still works (watch red).

Commit: `fix(backtest,signals): null-honest zero-scored aggregates; generate_signals honors allowlist; ms-epoch guard`

### Task 3: TickerCombobox UX

**Files:** Modify `platform/src/components/shared/TickerCombobox.tsx`; Test: extend `platform/src/components/shared/tickerCombobox.test.ts` + `platform/tests/ticker-combobox.spec.ts`.

Items:
1. **Enter picks the top SEARCH hit when a query is typed**: with non-empty `debounced` query and the user hasn't arrowed (selection untouched), Enter selects the first search row, not `flat[0]` (first quick-pick). Arrowed selection still wins. Implement by initializing/adjusting the selection index to the first search row's flat position whenever a fresh results set arrives and the user hasn't navigated (track a `userNavigated` flag reset on query change). vitest on an extracted pure `defaultSelectionIndex(quickCount, recentCount, searchCount, queryNonEmpty)` helper + Playwright: type "aa" → Enter → AAPL selected (currently IWM — watch red).
2. **Search self-dedupe**: `dedupeSearchResults` also dedupes WITHIN the results (first occurrence wins). vitest case: API returns AAPL twice → one row.
3. **"No matches" copy when deduped into chips**: when raw results were non-empty but all deduped away, render "Matches shown above" instead of "No matches for 'X'". vitest/Playwright per whichever layer the branch lives in (it's render logic — Playwright: type a quick-pick symbol "IWM" → the results section shows "Matches shown above").

Commit: `fix(ui): combobox Enter selects top search hit; self-dedupe; honest deduped-copy`

### Task 4: Frontend polish batch

**Files:** Modify `platform/src/routes/JournalPage.tsx` (emptyForm), `platform/src/routes/DashboardPage.tsx` (isoPlusDaysFrom), `platform/src/lib/journalStats.ts` (+ its test), `platform/src/components/primitives/index.tsx` (Delta), `platform/src/routes/AdminPage.tsx` (useCallback), `platform/src/routes/ChartsPage.tsx` (ticker-switch reset); Tests: extend `platform/src/routes/journalStats.test.ts`, `platform/src/lib/dates.test.ts` if new helper surface, vitest for Delta if it has a test precedent (MovementRead-style component test) else Playwright-adjacent assertion, extend `platform/tests/charts-cards.spec.ts` for the reset.

Items:
1. **`emptyForm()` dates → `todayET()`** (JournalPage:86,89). vitest: fake timers at 02:30Z → form default date is the ET yesterday-equivalent... simplest: export `defaultFormDates()` and test it mirrors `todayET()`.
2. **`isoPlusDaysFrom` → `addDaysToISO`**: DashboardPage's local UTC-arithmetic duplicate folds onto the shared lib helper (keep the local name delegating or replace call sites; existing behavior identical — both UTC-anchored). Gate: vitest suite + tsc (behavior-identical refactor; the dates.test.ts already pins addDaysToISO).
3. **`computeJournalStats` exposes `winCount`/`lossCount`**: add to the interface; JournalPage's call-site recompute (the W/L sub-label filter) switches to it. vitest: counts correct incl. null-return exclusion (watch red on the missing fields).
4. **Delta renders '—' when both value and pct are null** (currently blank). Test at whatever layer is cheapest — a component vitest (jsdom pragma, render, assert textContent '—') following MovementRead.test.tsx's setup.
5. **AdminPage `onLogout` in `useCallback`** (effect-deps hygiene). Gate: tsc + existing suites (no behavior change).
6. **My-style + replay-scorecard reset on ticker switch**: ChartsPage render-adjustment on `activeTicker` change calls `mineMyStyle.reset()` and closes/clears the scorecard state (the repo's `lastTicker` compare-prev idiom — BacktesterSection precedent). Playwright: mine style for IWM (mocked) → switch ticker via combobox (mock second ticker's routes) → my-style result gone, button back to idle (watch red).

Commit: `fix(ui): ET form defaults; shared day-math; winCount in stats; Delta em-dash; ticker-switch resets`

### Task 5: Docs/comments/audit-tag batch (no behavior)

**Files:** `platform/api/routers/backtest.py` (top docstring: `?run=` param + the playbook win_rate-percent exception note), `platform/api/main.py` (coverage 50-symbol cap docstring note), `gcp/fetchers/fetch_market_data.py` (stale `alert_config.json` comment at the watchlist-union site → `watchlists` table), `platform/src/components/options/SwingMode.tsx` (TickerSelect→TickerCombobox prose ×2), `platform/src/routes/ChartsPage.tsx` (`pnl ?? 0` exit-marker line gets `// AUDIT-2026-05-13: silent fallback — pre-existing, only reachable via manual DB writes`), `platform/api/routers/live.py` (`_float` gets the same AUDIT tag referencing docs/audits/FALLBACK_AUDIT_2026-05-13.md).

- [ ] Comment/doc edits only — TDD N/A (no runtime behavior). Gates: `npx tsc --noEmit`, `python -m pytest tests/test_backtest_router_units.py -q`, `npx vitest run` — all green proving nothing broke.

Commit: `docs: convention notes, audit tags, and stale-comment cleanup from #702`

### Task 6: Shared e2e mock fixtures

**Files:** Modify `platform/tests/helpers/mocks.ts` (add `mockDashboard(page)`, shared `MOCK_GRID`/`MOCK_LEVELS` exports), `platform/tests/dashboard.spec.ts`, `platform/tests/dashboard-chart-fit.spec.ts` (also FIX its wrong-shape `/api/backtest/*` mocks to the real response shapes), `platform/tests/navigation.spec.ts`, `platform/tests/demo-banners.spec.ts`.

- [ ] Test-only refactor. The gate IS the suites: run each touched spec before (green baseline) and after (still green, same test counts). No `.skip`s introduced; assert with `grep -c "test(" per file unchanged`.

Commit: `test(e2e): shared dashboard/options mock fixtures; correct backtest mock shapes`

### Task 7: TIMESTAMPTZ round-trip integration test

**Files:** Create/extend the integration-suite home for DB-backed tests — READ `.github/workflows/*.yml`'s "Integration Tests (ephemeral Postgres)" job first to learn how integration tests detect/connect to Postgres (env var? marker?), and follow that exact pattern; likely `tests/test_integration*.py`.

- [ ] One test: through the REAL journal router against the ephemeral Postgres — POST a trade (naive-ET entry_date/time), read it back via GET, assert the wall-clock round-trips exactly ('2026-07-02' + '10:15' → entry_ts string starting '2026-07-02 10:15' after the AT TIME ZONE 'UTC' read), then match it against the bar-matching format `strftime('%Y-%m-%d %H:%M')` used by replay. Skips cleanly (existing suite convention) when no Postgres is available locally — the CI job is the authoritative runner.

Commit: `test(integration): pin journal TIMESTAMPTZ round-trip to naive-ET wall clock`

---

## Final verification
- [ ] `python -m pytest tests/test_journal_phase2.py tests/test_replay_labeled_trades.py tests/test_style_walk_forward.py tests/test_live_signal_series.py tests/test_backtest_router_units.py -q` green
- [ ] `cd platform && npx tsc --noEmit && npx vitest run` green
- [ ] Playwright: ticker-combobox, charts-cards, journal, dashboard, navigation, demo-banners, replay-trainer suites green
- [ ] Close #702 with a comment mapping each item → commit.
