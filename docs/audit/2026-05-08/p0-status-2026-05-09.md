# Track A + E P0 Status — 2026-05-09

Cross-tracks Track A (foundation health) and Track E (per-ticker
calibration) — the P0 backlog from
[`audit-summary.md`](audit-summary.md) and
[`track-G.md`](track-G.md).

**All Track A + E P0s are now landed on `main`.** This doc records
which PRs shipped what, what's ops-only follow-up, and what was a
duplicate of work another track already shipped.

---

## P0 items — what shipped

| Audit item | Description | Shipped via | Notes |
|---|---|---|---|
| **G.P0.1** | Unfreeze daily fetcher (clear sticky `--date=2026-04-27` arg + 17-day backfill) | **#321** + parallel ops | Cleared via `--args=""` (NOT `--clear-args` — see runbook); 17 days backfilled for SPY/IWM/QQQ |
| **G.P0.2** | Fail-fast on stale `--date` and zero-row fetches | **#322** | exit 4 on stale date (>5d back); exit 5 on zero key-ticker rows; uses `= ANY(:tickers)` to bind under pg8000 |
| **G.P0.3** | Re-enable Freshness Watchdog + NULL-close-aware filter | **#323** | Workflow re-enabled live; `audit_data_freshness.py` now requires `close IS NOT NULL` so pre-market placeholder rows can't mask staleness |
| **G.P0.4** | Brief stale-warn guard | (Track B PR #293) | Already shipped before audit close-out |
| **G.P0.6** | `signal_alerts.conditions_met` JSONB writer fix + backfill | (Track C/D PR #308) | Already shipped 2026-05-08 |
| **G.P0.8** | Wire `daily_trades` + `daily_pnl` increments | (Track D PR #315) | Already shipped 2026-05-08 |
| **G.P0.9** | Move plaintext API keys to `--set-secrets` | (Track F PR #318) | Already shipped 2026-05-08 |
| **G.P0.10** | EOD reconciliation Cloud Run Job | (Track D PR #319) | Already shipped 2026-05-08; this track's PR-A4 (#324) was duplicate work, closed without merging |
| **G.P0.11** | Momentum strategy considered-vs-fired analysis | **#330** + (Track D PR #320) | Track D's instrumentation half (#320) shipped 2026-05-08; analysis half (#330) ships replay-based eligibility report. Pair the two for full closure. |
| **G.P0.12** | Drop `above_vwap` from MR PUT scoring (global) | **#329** | Audit measured −16.1pp (QQQ) / −11.7pp (IWM) / −9.9pp (SPY) ANTI-correlation. Removed from both `lib/signals.py:check_put_conditions` and `lib/strategies/mean_reversion.py:_check_put_conditions` |
| **G.P0.13** | Drop `stoch_rsi_overbought` + `rsi_overbought_zone` (IWM/QQQ only) | **#329** | Per-ticker drop list lives in `exit_config_overrides.disabled_conditions` JSONB; SPY conditions intact (only weak negative measured) |
| **G.P0.14** | Per-ticker ExitConfig overrides | **#326** + **#327** | New `exit_config_overrides` Cloud SQL table seeded with audit-recommended values for SPY/IWM/QQQ; `lib/strategies/exit_config_overrides.py` resolver mirrors `calibration.py` Tier-A → Tier-B fallback. Wired into `signal_monitor.fire_alert` and `lib/backtest.py` |
| **G.P1.17** | `data_loader` staleness check (pulled into Phase 1) | **#325** | New `on_stale='silent'/'warn'/'error'` parameter; `value_col='Close'` filters NULL-close placeholder rows before computing max date — codex review caught the placeholder-masking bug |

## Ops-only — completed in parallel with PR-A1

- Stale `--date=2026-04-27` arg cleared from `fetch-market-data` Cloud
  Run Job via `gcloud run jobs update fetch-market-data --args=""`
  (NOT the documented-but-nonexistent `--clear-args` — caught in PR #321 review).
- 17 missing dates backfilled for SPY/IWM/QQQ over the 2026-04-14 →
  2026-04-23 and 2026-04-28 → 2026-05-08 windows.
- Verification SQL dispatched via `db-query.yml` workflow.

## Cross-track close-outs

- **Issue #311** (G.P0.6 dependency tracker): closed — work shipped
  via PR #308 before issue was filed.
- **Issue #312** (G.P0.11 instrumentation half): closed — work
  shipped via Track D's PR #320 before issue was filed.
- **PR #324** (PR-A4 EOD reconciler): closed without merging —
  duplicate of Track D's PR #319 (already on `main`). 270 LOC, less
  elaborate than main's 412 LOC; codex review caught two correctness
  bugs that may or may not apply to main's version (rsi_14 column
  reference + UTC/ET TZ handling) — flagged in the close comment for
  potential follow-up if drift is suspected.

## What's still open

The three P0 items NOT in Track A+E remained the property of their
owning tracks: G.P0.5 (insights direction filter — Track C),
G.P0.7 (signal-monitor row write contract — Track D), and any
remaining items NOT enumerated as P0 in Track G's synthesis.

P1 backlog (G.P1.x) and P2/P3 backlog have NOT been touched by this
sprint. Per the recommendation in `audit-summary.md` "next sprint",
the P1 fixes should now be unblocked since G.P0.1 cleared the data
freeze that was masking signal quality.

## Cross-references

- Synthesis: [`track-G.md`](track-G.md) (the prioritized backlog)
- One-page summary: [`audit-summary.md`](audit-summary.md)
- Track A audit: [`track-A.md`](track-A.md)
- Track E audit: [`track-E.md`](track-E.md) +
  [`per_ticker_writeup.md`](per_ticker_writeup.md)
- Recommended config seeds:
  [`recommended_per_ticker_config.json`](recommended_per_ticker_config.json)
- Momentum eligibility report:
  [`momentum_eligibility_report.md`](momentum_eligibility_report.md)
