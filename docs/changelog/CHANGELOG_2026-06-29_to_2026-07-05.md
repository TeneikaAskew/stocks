# Changelog: 2026-06-29 to 2026-07-05

Diagram-refresh week: the three architecture diagrams (ERD, GCP
architecture, frontend) get repaired, icon-styled, and expanded with
process-flow pages. No code changes.

## Themes this week

1. **ERD becomes real tables.** The hand-authored ERD had six corrupt
   nested edge-label cells that made three tabs error on open; fixed,
   then every entity box converted to a proper drawio table shape.
2. **Icon diagrams land in-repo.** The icon/process-flow rebuilds of the
   GCP architecture and frontend diagrams (previously only in Downloads)
   are merged with the repo versions' unique content and committed as
   new `-icons` files alongside the originals.

---

## ERD.drawio — corruption fix + table-shape conversion

- **Date**: 2026-07-05
- **Files**: `ERD.drawio`
- **What changed**: Six edge-label `<mxCell>` elements were nested inside
  their parent edge cells with no `id` — invalid mxGraph XML that made
  drawio fail with "Could not add object for mxCell" when opening the
  Market data, Insights, and Backtest tabs. Labels were hoisted to
  sibling cells with unique ids and standard `edgeLabel` style. All 61
  entity boxes across the 6 pages were then converted from newline-text
  rectangles to real drawio table shapes: title bar, PK/UQ/FK/`-` badge
  column with a full-height divider, one row per field, italic
  writer/footer rows. Category border colors preserved; each page's
  layout reflowed downward (column-aware push-down) so nothing overlaps.
- **Why**: The nested-label corruption shipped in PR #535's hand-authored
  XML; the text-blob boxes didn't read like an ER diagram.
- **Benefit**: All tabs open cleanly; tables look and behave like tables
  (rows are selectable/editable in drawio).

---

## Architecture-icons.drawio — merged icon architecture diagram (new file)

- **Date**: 2026-07-05
- **Files**: `Architecture-icons.drawio` (new; `Architecture.drawio`
  untouched)
- **What changed**: The 2026-06-23 icon rebuild (GCP2 stencils + the
  "one trading day" process-flow page, previously only in Downloads) is
  now in-repo, merged with the repo diagram's unique content: a new
  `8 · Earnings Pipeline` page rebuilt as swim-lanes with numbered steps
  and Cloud Scheduler/Run/SQL icons (including the fetch-earnings-options
  CONFIRMED BROKEN warning). Backtest page brought current: PR #519
  timeframe-combo matrix + --sweep-only, PR #547 trend-filter EMA param,
  PR #548 walk-forward validation + backtest_walk_forward_folds table.
- **Why**: Best-of-both — icons + true process flow + full page coverage.
- **Benefit**: One canonical visual architecture with all 9 pages;
  reconciliation notes record the merge provenance.

---

## Frontend-icons.drawio — 15-tab frontend diagram with per-route data flows (new file)

- **Date**: 2026-07-05
- **Files**: `Frontend-icons.drawio` (new; `Frontend.drawio` untouched)
- **What changed**: Base is the 2026-06-23 icon rebuild (Process Flow +
  overview pages). Added `2 · Auth & Entry` — the real login flow (Google
  IAP SSO, no Firebase/login screen; app boot chain; TokenGate admin
  path; local-dev fallback) with a caveats box (plaintext IAP-header
  trust, hardcoded ADMIN_EMAIL, sidebar-hide is cosmetic). Added one tab
  per route (12 tabs, Dashboard → Help), each with Lane A user flow,
  Lane B data-in (hook → endpoint → router → tables READ + external
  tiles for AlphaVantage/Benzinga/FinViz/Vertex/GCS), and Lane C
  data-out (the four real write paths: journal CRUD, insight refresh via
  Cloud Tasks, watchlist add/remove, model_routing upsert). Content
  transcribed from a fresh codebase sweep, not the old diagram. Overview
  limitations box extended with the failure-notifier sink caveat and the
  --set-secrets list.
- **Why**: Per-page visibility into what data each screen touches, where
  it comes from, and where writes go.
- **Benefit**: Every app page now has an accurate, current data-flow tab.
